import cafe.adriel.voyager.core.screen.Screen

abstract class ApplicationSlot: Screen{
    var screenTitle

    @Composable
    fun setScreen(screenTitle: String, contents: List<T>){
        screenTitle = screenTitle;
        for(content in contents){
        }
    }


    //TODO: This needs to be adapted. It needs to be generic so it can be used for many application-slots instead of just one specific.
    @override Content(title: String, content: List){ // Todo: maybe one more argument for placement?
        TopAppBarWithBackButton(screenTitle)
        Column(
            modifier = Modifier.fillMaxSize(),
            //Todo: need to find some way to arrange all the applications.
            verticalArrangement = Arrangement.Center,
            horizontalAlignment = Alignment.CenterHorizontally
        ){
            val relativeHumidity = 0.0
            Text("Relative Humidity: ${relativeHumidity}%")
        }
    }

    abstract fun getTitle()

    abstract fun getStatus()

    abstract fun setStatus()
}
